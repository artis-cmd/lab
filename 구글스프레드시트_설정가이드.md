# 구글 스프레드시트 API 설정 가이드

## 📋 개요
파일명 변경 로그를 구글 스프레드시트에 자동으로 기록하는 기능을 사용하기 위한 설정 가이드입니다.

## 🔧 설정 단계

### 1. 구글 클라우드 콘솔 설정

#### 1-1. 프로젝트 생성
1. [Google Cloud Console](https://console.cloud.google.com/)에 접속
2. 새 프로젝트 생성 또는 기존 프로젝트 선택
3. 프로젝트 이름: `파일명변경로그` (예시)

#### 1-2. Google Sheets API 활성화
1. 왼쪽 메뉴 → "API 및 서비스" → "라이브러리"
2. "Google Sheets API" 검색 후 활성화

#### 1-3. 사용자 인증 정보 생성
1. "API 및 서비스" → "사용자 인증 정보"
2. "사용자 인증 정보 만들기" → "OAuth 2.0 클라이언트 ID"
3. 애플리케이션 유형: "웹 애플리케이션"
4. 승인된 JavaScript 원본: 
   - `http://localhost:8003`
   - `http://localhost:5000`
   - `http://192.168.1.99:5000` (실제 사용 IP)
5. 승인된 리디렉션 URI: 
   - `http://localhost:8003`
   - `http://localhost:5000`
   - `http://192.168.1.99:5000` (실제 사용 IP)

#### 1-4. API 키 생성
1. "사용자 인증 정보 만들기" → "API 키"
2. 생성된 API 키 복사

### 2. 스프레드시트 생성

#### 2-1. 새 스프레드시트 생성
1. [Google Sheets](https://sheets.google.com/)에서 새 스프레드시트 생성
2. 스프레드시트 제목: `파일명변경로그` (예시)

#### 2-2. 헤더 설정
첫 번째 행에 다음 헤더를 입력:
```
A1: 타임스탬프
B1: 원본파일명
C1: 새파일명
D1: 저장폴더
E1: 변경일시
```

#### 2-3. 권한 설정
1. 우상단 "공유" 버튼 클릭
2. **"링크가 있는 모든 사용자"** → **"편집자"** 권한 부여
3. **"완료"** 버튼 클릭
4. 스프레드시트 URL에서 ID 복사 (URL의 `/d/`와 `/edit` 사이의 문자열)

**⚠️ 중요**: 스프레드시트가 **"링크가 있는 모든 사용자"**에게 **"편집자"** 권한이 있어야 API 키로 접근할 수 있습니다.

### 3. 코드 설정

#### 3-1. API 키 및 클라이언트 ID 입력
`HTML/js/CapName.js` 파일에서 다음 부분을 수정:

```javascript
gapi.client.init({
    'apiKey': 'AlzaSyAzbzatYXC_DekGlhKftfvJ-fJgGpHqqLk',
    'clientId': '611762397046-5ifa9sa1r2us4t3b0hs2acj63nc6tuu8.apps.googleusercontent.com',
    'scope': 'https://www.googleapis.com/auth/spreadsheets',
    'discoveryDocs': ['https://sheets.googleapis.com/$discovery/rest?version=v4']
})
```

**✅ 설정 완료된 정보:**
- **API 키**: `AlzaSyAzbzatYXC_DekGlhKftfvJ-fJgGpHqqLk`
- **클라이언트 ID**: `611762397046-5ifa9sa1r2us4t3b0hs2acj63nc6tuu8.apps.googleusercontent.com`
- **클라이언트 보안 비밀번호**: `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` (백업용)

#### 3-2. 스프레드시트 설정
웹 페이지에서:
1. "스프레드시트 ID" 필드에 복사한 ID 입력
2. "시트 이름" 필드에 "Sheet1" (기본값) 또는 원하는 시트명 입력
3. "연결 테스트" 버튼 클릭

## 📊 로그 형식

스프레드시트에 기록되는 로그 형식:

| 컬럼 | 설명 | 예시 |
|------|------|------|
| A | 타임스탬프 | 2025-01-07T10:30:00.000Z |
| B | 원본파일명 | screen_45932.png |
| C | 새파일명 | 20250807d_1311_TK_복기_XAUUSD_FT5_L1_Range.png |
| D | 저장폴더 | 복기_이미지 |
| E | 변경일시 | 2025. 1. 7. 오후 7:30:00 |

## 🔍 사용 방법

### 1. 초기 설정
1. 구글 계정으로 로그인
2. 스프레드시트 ID 입력
3. "연결 테스트" 버튼으로 연결 확인
4. "파일명 변경 로그 기록" 체크박스 활성화

### 2. 파일명 변경 시
1. 원본 폴더에서 파일 선택
2. 저장 폴더 지정
3. "선택된 파일 이름 변경" 버튼 클릭
4. 자동으로 스프레드시트에 로그 기록

## ⚠️ 주의사항

1. **API 할당량**: Google Sheets API는 일일 할당량이 있습니다
2. **인증**: 처음 사용 시 구글 계정 인증이 필요합니다
3. **권한**: 스프레드시트에 편집 권한이 있어야 합니다
4. **네트워크**: 인터넷 연결이 필요합니다

## 🛠️ 문제 해결

### 연결 실패 시
1. API 키와 클라이언트 ID 확인
2. 스프레드시트 ID 확인
3. 스프레드시트 권한 확인
4. 브라우저 콘솔에서 오류 메시지 확인

### 로그 기록 실패 시
1. 구글 계정 로그인 상태 확인
2. 스프레드시트 접근 권한 확인
3. 시트 이름 확인
4. 네트워크 연결 확인

## 📈 활용 방안

1. **파일 관리 통계**: 변경된 파일 수, 빈도 등 분석
2. **작업 이력 추적**: 언제 어떤 파일을 변경했는지 확인
3. **백업 및 복구**: 변경 이력을 통한 파일 복구
4. **팀 협업**: 여러 사용자의 파일 변경 이력 공유
